<h1>Listing metros</h1>

<table>
  <tr>
    <th>Name</th>
    <th>String</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @metros.each do |metro| %>
  <tr>
    <td><%= metro.name %></td>
    <td><%= metro.string %></td>
    <td><%= link_to 'Show', metro %></td>
    <td><%= link_to 'Edit', edit_metro_path(metro) %></td>
    <td><%= link_to 'Destroy', metro, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Metro', new_metro_path %>
