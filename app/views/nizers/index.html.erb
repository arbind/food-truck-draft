<h1>Listing nizers</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Value</th>
    <th>Approved</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @organizers.each do |organizer| %>
  <tr>
    <td><%= nizer.name %></td>
    <td><%= nizer.value %></td>
    <td><%= nizer.approved %></td>
    <td><%= link_to 'Show', nizer %></td>
    <td><%= link_to 'Edit', edit_nizer_path(nizer) %></td>
    <td><%= link_to 'Destroy', nizer, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Nizer', new_nizer_path %>
